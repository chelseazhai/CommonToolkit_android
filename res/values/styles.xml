<resources>

    <!-- application default theme -->
    <style name="AppTheme" parent="android:Theme.Light.NoTitleBar" />

    <!-- application launch theme -->
    <style name="AppLaunchTheme" parent="android:Theme.Black.NoTitleBar.Fullscreen" />

</resources>